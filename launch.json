{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Django Runserver",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": [
                "runserver"
                // Opcional: Especificar puerto, ej. "8080"
            ],
            "django": true,
            "justMyCode": true,
            "env": {
                // Si necesitas variables de entorno específicas
            }
        }
    ]
}